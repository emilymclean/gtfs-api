syntax = "proto2";

option java_package = "cl.emilym.gtfs.tripindex";

package trip_index;

message TripIndex {
    map<string, TripInformation> trips = 1;
}

message TripInformation {
    required string routeId = 1;
    repeated string stopIds = 2;
}